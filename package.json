{
  "name": "openclaw-fuel-gauge-poc",
  "version": "0.0.1",
  "description": "Unofficial OpenClaw Control UI fuel gauge PoC: manual refresh button for remaining provider capacity.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "keywords": [
    "openclaw",
    "control-ui",
    "usage-limits",
    "rate-limits",
    "claude-code",
    "codex",
    "plugin",
    "userscript",
    "agent-ui",
    "quota"
  ],
  "openclaw": {
    "extensions": ["./index.js"],
    "compat": {
      "pluginApi": ">=2026.4.0",
      "minGatewayVersion": ">=2026.4.0"
    }
  },
  "scripts": {
    "check": "node --check index.js && node --check src/config.js && node --check src/collector.js && node --check userscript/openclaw-fuel-gauge.client.js && node --check userscript/openclaw-fuel-gauge.user.js",
    "collect": "node ./src/collector.js",
    "smoke": "node ./src/collector.js --pretty"
  }
}
